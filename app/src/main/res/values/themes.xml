<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BirdVerify" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>

        <!-- Add this to apply a dark background to the activity -->
        <item name="android:background">@color/cardview_dark_background</item>

        <!-- Add this to customize the text color in your activity -->
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.BirdVerify" parent="Base.Theme.BirdVerify" />

    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- ... -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:colorBackground">@color/dark_gray</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/light_gray</item>
        <item name="android:navigationBarColor" tools:targetApi="o_mr1">@color/light_gray</item>
    </style>

    <style name="AppTheme.Light" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- ... -->
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:colorBackground">@android:color/white</item>
    </style>

</resources>